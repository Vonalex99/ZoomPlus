<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zoom">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_zoom_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_zoom_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AskPermissionActivity" android:theme="@style/AppTheme.Pop">
        </activity>
        <activity
            android:name=".NewMeetingActivity"
            android:label="@string/title_activity_new_meeting"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>